project:
  name: ml-engineering-master
  version: "0.1.0"
  seed: 42

data:
  raw_path: data/raw/
  processed_path: data/processed/
  test_size: 0.2

model:
  learning_rate: 0.01
  max_iter: 100
  random_state: 42

logging:
  level: INFO
